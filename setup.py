# from setuptools import setup, find_packages

# setup(
#      name="ecommerce_ml_system",
#      version="0.1",
#      packages=find_packages(),
#      )

from pathlib import Path
print(Path(__file__).resolve().parent)